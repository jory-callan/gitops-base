# CloudNativePG 通过 pgBackRest（barman）备份到 MinIO
# 引用 MinIO 已有凭据，避免硬编码
apiVersion: v1
kind: Secret
metadata:
  name: minio-backup-creds
  namespace: postgres
type: Opaque
stringData:
  access-key: minioadmin
  secret-key: minioadmin
