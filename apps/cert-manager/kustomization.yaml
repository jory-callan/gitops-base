apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: cert-manager
    repo: http://192.168.5.103:8081/repository/helm-hosted/
    version: v1.20.2
    releaseName: cert-manager
    namespace: cert-manager
    valuesFile: values.yaml
    includeCRDs: true

resources:
  - cluster-issuer.yaml
  - service-monitor.yaml
  - resourcequota.yaml
